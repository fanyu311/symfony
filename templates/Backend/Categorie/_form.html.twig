{{ form_start(form, {'attr': {'class': 'form mt-4 card p-3 w-50 mx-auto'}}) }}
{{ form_widget(form) }}
<div class="text-center">
    <button type="submit" class="btn btn-primary">{{ buttonLabel|default('save') }}</button>
</div>
{{ form_end(form) }}