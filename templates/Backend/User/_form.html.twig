{{ form_start(form, {'attr': {'class': 'form card p-3 w-50 mx-auto mt-4'}}) }}
{{ form_widget(form) }}
<div class="text-center">
	<button type="submit" class="btn btn-primary">{{ buttonLabel|default('save') }}</button>
</div>
{{ form_end(form) }}
